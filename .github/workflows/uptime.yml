name: Uptime Ping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Render awake
        run: curl -sf --max-time 10 https://triad-duel-server.onrender.com/ || true
