name: Uptime Ping

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Render awake
        run: curl -s https://triad-duel-server.onrender.com/api/new-campaign -X POST || true
